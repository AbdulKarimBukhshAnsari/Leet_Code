class Solution:
    def minPatches(self, nums: list[int], n: int) -> int:
        lst = nums[:]
        result = 0
        while [i for i in range(1,n+1)] != nums:
            for i in range(len(lst)):
                pass
from itertools import combinations

def find_possible_sums(elements):
    return {sum(pair) for pair in combinations(elements, 2)}

# Example usage
elements = [1, 2, 3]
possible_sums = find_possible_sums(elements)
print(list(set(elements+list(possible_sums))))

print({1,2,3}|{2,3,4})

        